export class Message {
 status: string;
 message: string;
 reply: string;
 constructor(status: string, message: string, reply: string) {
  this.status = status;
  this.message = message;
  this.reply = reply;
 }
}

class Results {
  perplexity: string ;
  reply: string ;
 }

